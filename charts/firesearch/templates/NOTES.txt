1. Firesearch has been deployed.

{{- if .Values.ingress.enabled }}

Access it via your configured ingress host(s):
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}

Get the application URL:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "firesearch.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "http://$NODE_IP:$NODE_PORT"
{{- else }}

The Service is ClusterIP (internal). To reach it locally, port-forward:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "firesearch.fullname" . }} 3000:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:3000"
{{- end }}

{{- if not .Values.secrets.existingSecret }}
{{- if not (and .Values.secrets.openaiApiKey .Values.secrets.firecrawlApiKey) }}

WARNING: API keys were not provided. Set them with:
  --set-string secrets.openaiApiKey=... --set-string secrets.firecrawlApiKey=...
or point to an existing Secret with --set secrets.existingSecret=<name>.
{{- end }}
{{- end }}
